appVersion: apps/v1
kind: Deployment
metadata:
  name: testing-app
spec:
  selector:
    matchLabels:
      app: testing-app
  replicas: 3
  template:
    metadata:
      labels:
        app: testing-app
    spec: 
      containers:
      - name: testing-app
        image: nginx:latest
        ports:
        - containerPort: 80